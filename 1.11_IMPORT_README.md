# 1.11数据汇总表批量导入脚本使用说明

## 📋 脚本概述

`optimized_batch_import.mjs` 是一个优化的批量导入脚本，专门用于将AI Drive中的1.11数据汇总表CSV文件批量导入到生产环境的商品数据库。

### ✨ 主要特性

- ✅ **批量导入**: 支持8个分割文件（part1-part8）的连续导入
- 🔄 **断点续传**: 支持中断后从上次位置继续导入
- 📊 **实时进度统计**: 显示文件进度、分块进度、导入记录数等
- 🧠 **智能分块**: 根据文件行数自动调整分块大小
- 📝 **详细日志**: 所有操作记录到日志文件
- 🛡️ **错误重试**: 自动重试失败的请求（最多3次）
- 🎯 **后台运行**: 支持后台执行，不阻塞终端

## 📁 目标文件

脚本将导入以下8个文件（位于`/mnt/aidrive/`）：

```
1.11数据汇总表-utf8_part1.csv (26.4 KB, ~400行)
1.11数据汇总表-utf8_part2.csv (26.6 KB, ~400行)
1.11数据汇总表-utf8_part3.csv (28.5 KB, ~430行)
1.11数据汇总表-utf8_part4.csv (30.1 KB, ~460行)
1.11数据汇总表-utf8_part5.csv (30.0 KB, ~460行)
1.11数据汇总表-utf8_part6.csv (29.8 KB, ~450行)
1.11数据汇总表-utf8_part7.csv (29.8 KB, ~450行)
1.11数据汇总表-utf8_part8.csv (29.8 KB, ~450行)
```

**预计总数据量**: ~231 KB，约3500条记录

## 🚀 使用方法

### 方式一：前台运行（查看实时输出）

```bash
cd /home/user/webapp && node optimized_batch_import.mjs
```

### 方式二：后台运行（推荐）

```bash
cd /home/user/webapp && nohup node optimized_batch_import.mjs > import_1.11_output.log 2>&1 &
```

### 查看后台进程

```bash
# 查看进程
ps aux | grep optimized_batch_import

# 查看实时日志
tail -f import_1.11_output.log

# 或查看脚本生成的详细日志
tail -f 1_11_import.log
```

### 停止后台进程

```bash
# 查找进程ID
ps aux | grep optimized_batch_import

# 终止进程
kill <PID>
```

## 📊 进度监控

### 实时查看导入进度

脚本运行时会生成以下文件：

1. **`1_11_import.log`** - 详细日志文件
   ```bash
   tail -f 1_11_import.log
   ```

2. **`1_11_import_stats.json`** - 统计数据（JSON格式）
   ```bash
   cat 1_11_import_stats.json | jq
   ```

3. **`1_11_import_progress.json`** - 进度信息（用于断点续传）
   ```bash
   cat 1_11_import_progress.json | jq
   ```

### 统计数据示例

```json
{
  "totalFiles": 8,
  "processedFiles": 3,
  "currentFile": "1.11数据汇总表-utf8_part4.csv",
  "totalRecords": 3500,
  "processedChunks": 12,
  "totalChunks": 35,
  "importedRecords": 1200,
  "startTime": "2026-01-11T10:30:00.000Z",
  "status": "running",
  "estimatedTimeRemaining": 180
}
```

### 进度信息说明

- **totalFiles**: 总文件数（8个）
- **processedFiles**: 已完成的文件数
- **currentFile**: 当前正在处理的文件
- **totalChunks**: 总分块数
- **processedChunks**: 已完成的分块数
- **importedRecords**: 已导入的记录数
- **estimatedTimeRemaining**: 预计剩余时间（秒）

## 🔄 断点续传

如果脚本在运行过程中被中断，再次运行脚本会自动从上次停止的位置继续：

```bash
# 脚本会自动检测进度文件并继续导入
cd /home/user/webapp && node optimized_batch_import.mjs
```

日志会显示：
```
🔄 检测到断点续传: 已完成 3 个文件
📍 从第 4 个文件开始继续导入...
```

## ⚙️ 配置参数

脚本中的关键配置（可根据需要调整）：

```javascript
const PRODUCTION_URL = 'https://webapp-csv-import.pages.dev'; // 生产环境地址
const USERNAME = 'admin';                    // 管理员用户名
const PASSWORD = 'admin123';                 // 管理员密码
const MAX_RETRIES = 3;                       // 最大重试次数
const DELAY_BETWEEN_CHUNKS = 600;            // 分块间延迟(ms)
const DELAY_BETWEEN_FILES = 2000;            // 文件间延迟(ms)
const PROGRESS_SAVE_INTERVAL = 3;            // 每N个分块保存进度
```

## 🧠 智能分块策略

脚本根据文件实际行数自动调整分块大小：

- 超大文件（>10万行）→ 100行/块
- 大文件（5-10万行）→ 120行/块
- 中等文件（1-5万行）→ 150行/块
- 小文件（1千-1万行）→ 200行/块
- 超小文件（<1千行）→ 300行/块

**1.11数据文件预计分块**: 每个文件约400-460行，预计使用300行/块策略，每个文件约2个分块。

## 📈 导入结果

导入完成后，脚本会显示总结信息：

```
🎉 1.11数据汇总表批量导入完成！
================================================================================
✅ 成功处理文件: 8/8
📈 成功导入记录: 3,500 条
📊 预期记录数: 3,500 条
🗄️ 数据库最终记录数: 125,500 条
⏱️ 总耗时: 15分30秒
📊 总体导入成功率: 100.00%
🚀 平均处理速度: 225 条/分钟

📋 各文件导入结果:
   ✅ 1.11数据汇总表-utf8_part1.csv: 400条记录导入成功
   ✅ 1.11数据汇总表-utf8_part2.csv: 400条记录导入成功
   ✅ 1.11数据汇总表-utf8_part3.csv: 430条记录导入成功
   ...
```

## 🛡️ 错误处理

### 常见错误及解决方案

1. **登录失败**
   ```
   ❌ 登录失败: 401 Unauthorized
   ```
   - 检查USERNAME和PASSWORD是否正确

2. **文件不存在**
   ```
   ❌ 缺失的文件:
      ❌ 1.11数据汇总表-utf8_part5.csv: File not found
   ```
   - 确认文件是否存在于`/mnt/aidrive/`
   - 检查文件名是否正确

3. **网络错误**
   ```
   ⚠️ 请求失败 (503)，重试中...
   ```
   - 脚本会自动重试（最多3次）
   - 检查生产环境是否可访问

4. **分块导入失败**
   ```
   ❌ 分块导入最终失败: Connection timeout
   ```
   - 检查网络连接
   - 可以重新运行脚本，会从失败位置继续

## 📝 日志文件说明

### 1_11_import.log

详细的操作日志，包含：
- 每个文件的处理进度
- 每个分块的导入结果
- 错误信息和重试记录
- 最终统计结果

### 1_11_import_stats.json

JSON格式的统计数据，包含：
- 文件级别的进度
- 分块级别的进度
- 导入的记录数
- 开始/结束时间
- 状态信息

### 1_11_import_progress.json

断点续传的进度信息，包含：
- 当前文件索引
- 当前分块索引
- 已完成的文件数
- 时间戳

## 🧹 清理临时文件

导入完成后，可以清理临时文件：

```bash
# 清理进度文件（如果不需要断点续传）
rm -f 1_11_import_progress.json

# 保留日志文件用于审计
# 如需清理日志文件：
# rm -f 1_11_import.log 1_11_import_stats.json
```

## ⏱️ 预计导入时间

基于以下假设：
- 8个文件，共约3500条记录
- 平均处理速度: 200-250条/分钟
- 每个文件间隔2秒

**预计总耗时**: 15-20分钟

## 🔧 故障排查

### 检查文件是否存在

```bash
ls -lh /mnt/aidrive/1.11数据汇总表-utf8_part*.csv
```

### 测试生产环境连接

```bash
curl https://webapp-csv-import.pages.dev/api/auth/login \
  -X POST \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

### 查看数据库记录数

```bash
# 在生产环境中查询
curl https://webapp-csv-import.pages.dev/api/products?page=1&pageSize=1 \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## 📞 支持

如有问题，请检查：
1. 日志文件 `1_11_import.log`
2. 统计文件 `1_11_import_stats.json`
3. 生产环境状态

## 🎯 下次使用

如需导入其他批次的数据，只需修改脚本中的以下配置：

```javascript
const TARGET_FILE_PREFIX = '1.XX数据汇总表-utf8_part';
const TARGET_FILES = [
  '1.XX数据汇总表-utf8_part1.csv',
  // ... 更多文件
];
const PROGRESS_FILE = './1_XX_import_progress.json';
const LOG_FILE = './1_XX_import.log';
const STATS_FILE = './1_XX_import_stats.json';
```

然后运行脚本即可。
