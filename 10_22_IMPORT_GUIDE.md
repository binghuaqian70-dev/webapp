# 10.22数据汇总表批量导入系统使用指南

## 🎯 概述

本系统支持从AI Drive批量导入10.22数据汇总表的10个分割文件到生产环境的商品数据库。

### 📁 目标文件
- `10.22数据汇总表-utf8_part1.csv` 到 `10.22数据汇总表-utf8_part10.csv`
- 文件存储位置：`/mnt/aidrive/`

## 🚀 快速开始

### 方法一：使用启动脚本（推荐）
```bash
# 运行用户友好的启动脚本
./start_10_22_import.sh
```

### 方法二：直接运行
```bash
# 直接运行导入脚本
node optimized_batch_import.mjs
```

### 方法三：后台运行
```bash
# 后台运行并保存日志
nohup node optimized_batch_import.mjs > 10_22_import.log 2>&1 &
```

## 📊 监控进度

### 实时状态检查
```bash
# 检查导入状态（推荐定期运行）
node check_10_22_import_status.mjs
```

### 查看日志
```bash
# 查看实时日志
tail -f 10_22_import.log

# 查看最近日志
tail -n 50 10_22_import.log
```

## ⚙️ 系统特性

### 🔄 智能导入策略
- **逐个文件导入**：按文件顺序逐一处理，避免资源过载
- **智能分块**：根据文件大小自动调整块大小（80-150行/块）
- **断点续传**：支持从中断点继续导入
- **重试机制**：失败时自动重试最多3次
- **进度持久化**：实时保存导入进度

### 📈 性能配置
- **分块间延迟**：1.2秒（多文件批量处理优化）
- **文件间延迟**：3秒（避免系统过载）
- **价格精度**：支持6位小数精度
- **并发控制**：单文件单线程处理

## 📋 文件说明

| 文件名 | 说明 |
|--------|------|
| `optimized_batch_import.mjs` | 主导入脚本 |
| `check_10_22_import_status.mjs` | 状态监控脚本 |
| `start_10_22_import.sh` | 用户友好启动脚本 |
| `10_22_import_stats.json` | 实时统计数据 |
| `10_22_import_progress.json` | 断点续传进度 |
| `10_22_import.log` | 详细导入日志 |

## 🔍 状态监控详解

### 导入状态类型
- **pending**: 等待开始
- **running**: 正在导入
- **completed**: 成功完成
- **completed_with_errors**: 部分成功
- **error**: 发生错误

### 监控指标
- 文件处理进度（x/10）
- 分块处理进度（x/总分块数）
- 已导入记录数
- 当前处理文件
- 预计剩余时间
- 数据库记录数变化

## 🛠️ 故障排除

### 常见问题

#### 1. 文件未找到错误
**现象**：提示找不到目标文件
**解决**：
```bash
# 检查AI Drive中的文件
ls -la /mnt/aidrive/10.22*

# 确保文件名准确匹配
```

#### 2. 导入进程异常退出
**现象**：状态显示运行中但无进程
**解决**：
```bash
# 检查进程状态
node check_10_22_import_status.mjs

# 重新启动（支持断点续传）
node optimized_batch_import.mjs
```

#### 3. 网络连接问题
**现象**：登录或API调用失败
**解决**：
- 检查网络连接
- 确认生产环境URL可访问
- 验证登录凭据

#### 4. 断点续传问题
**现象**：无法从断点继续
**解决**：
```bash
# 查看进度文件
cat 10_22_import_progress.json
cat 10_22_import_stats.json

# 如需重新开始，删除进度文件
rm 10_22_import_progress.json 10_22_import_stats.json
```

### 清理和重置

```bash
# 完全重置（删除所有进度和日志）
rm -f 10_22_import_*.json 10_22_import.log

# 仅重置进度（保留日志）
rm -f 10_22_import_progress.json
```

## 📊 预期性能

### 时间预估
- **单个文件**：根据记录数，通常1-5分钟
- **全部10个文件**：预计10-30分钟
- **具体时间**：取决于网络状况和文件大小

### 资源使用
- **内存**：中等使用量，分块处理避免大内存占用
- **网络**：稳定的网络连接，支持自动重试
- **磁盘**：临时日志和进度文件，占用空间很小

## 🎉 导入完成后

### 验证导入结果
```bash
# 检查最终状态
node check_10_22_import_status.mjs

# 查看导入统计
cat 10_22_import_stats.json | jq '.'
```

### 清理临时文件（可选）
```bash
# 导入成功后可以清理临时文件
rm -f 10_22_import_progress.json  # 进度文件会自动清理
# 保留日志和统计文件用于记录
```

## 📞 技术支持

如遇到问题：
1. 首先运行状态检查脚本获取详细信息
2. 查看日志文件了解具体错误
3. 参考本指南的故障排除部分
4. 记录错误信息和状态用于问题分析

---

**最后更新**: 2025-10-23  
**版本**: v1.0 - 10.22数据汇总表批量导入版本