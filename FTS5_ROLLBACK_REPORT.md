# FTS5 回滚报告

## 📅 时间
- **回滚时间**: 2025-12-01 04:16
- **原因**: FTS5 实现存在 SQL 变量限制问题

---

## ⚠️ 问题分析

### FTS5 失败原因
在启用 FTS5 全文索引后，100个测试案例中有 **53个失败 (53%)**，错误信息：

```
D1_ERROR: too many SQL variables at offset 365: SQLITE_ERROR
```

### 根本原因
1. **两步查询导致变量过多**:
   - 第1步：FTS5 查询返回大量 rowIds（如搜索 "15" 返回 6万个ID）
   - 第2步：`WHERE id IN (?, ?, ...)` 生成6万个占位符
   - SQLite 限制：单个查询最多 999-32766 个变量

2. **修复尝试**:
   - 使用 `JOIN` 直接查询：`FROM products_fts fts JOIN products p ON fts.rowid = p.id`
   - 仍然失败，可能是 D1 的 FTS5 实现问题

---

## ✅ 回滚方案

### 代码修改
```typescript
// 禁用 FTS5，使用智能 LIKE 搜索
const useFTS5 = false; // 临时禁用 FTS5
```

### 回滚结果
- **部署URL**: https://d6bfc072.webapp-csv-import.pages.dev
- **Git Commit**: `dd03f31` - "Rollback to smart LIKE search mode"

---

## 📊 性能对比

### 回滚后性能 (智能 LIKE 模式)
| 指标 | 数值 |
|------|------|
| **成功率** | 99% (99/100) |
| **平均响应** | 0.426s |
| **中位数** | 0.338s |
| **P95** | 0.657s |
| **最慢** | 1.071s |

**性能分布**:
- 优秀 (<0.3s): 36个 (36.4%)
- 良好 (0.3-0.5s): 21个 (21.2%)
- 一般 (0.5-0.8s): 41个 (41.4%)
- 较慢 (0.8-1.5s): 1个 (1.0%)

**评级**: ⭐⭐⭐⭐ 良好

---

## 🎯 当前优化状态

### ✅ 已完成优化
1. **智能搜索模式** (已部署)
   - 数字/英文/中文自动前缀搜索
   - 平均加速 3.8x
   - 中位数响应: 0.338s

2. **UptimeRobot 预热** (已配置)
   - 每5分钟 ping `/api/health`
   - 消除 48秒冷启动
   - 冷启动概率: 20% → <2%

3. **延迟 COUNT 查询**
   - `skipCount=true` 跳过总数查询
   - 首屏加载加速 1.4x

### ❌ FTS5 失败原因
- Cloudflare D1 的 FTS5 实现存在限制
- 需要进一步研究 D1 + FTS5 的正确用法
- 可能需要改用其他方案（如 KV + 倒排索引）

---

## 🔮 下一步优化建议

### P0: 稳定当前方案 (已完成)
- ✅ 回滚到智能 LIKE 搜索
- ✅ 保持 99% 成功率
- ✅ 平均响应 0.426s

### P1: 研究 FTS5 替代方案
**选项1: KV + 倒排索引**
- 在 CSV 导入时构建倒排索引
- 存储在 Cloudflare KV
- 查询流程: KV 获取ID列表 → D1 批量查询
- 预期性能: 0.05-0.1s

**选项2: 外部搜索服务**
- Algolia / Meilisearch / Typesense
- 需要额外成本
- 性能最优: 0.01-0.05s

**选项3: 优化 FTS5 实现**
- 研究 D1 + FTS5 的最佳实践
- 可能需要分批查询
- 风险较高，需要充分测试

### P2: KV 缓存热门搜索
- 缓存 Top 100 热门关键词
- 预期命中率: 60-80%
- 缓存命中响应: 0.01s

---

## 📈 性能路线图

| 阶段 | 方案 | 平均响应 | 成功率 | 状态 |
|------|------|---------|--------|------|
| **当前** | 智能 LIKE 搜索 | 0.426s | 99% | ✅ 已部署 |
| P1 | KV 倒排索引 | 0.08s | 99.9% | 🔄 研究中 |
| P2 | KV 缓存 | 0.05s | 99.9% | 📋 待实施 |
| 终极 | 外部搜索 | 0.02s | 99.99% | 💰 可选 |

---

## 📚 相关文档
- [100案例性能测试报告](./100_CASES_PERFORMANCE_TEST_REPORT.md)
- [智能搜索性能报告](./SMART_SEARCH_PERFORMANCE_REPORT.md)
- [冷启动与优化总结](./WHY_COLD_START_AND_OPTIMIZATION_SUMMARY.md)
- [Worker防回收指南](./KEEP_WORKER_ALIVE_GUIDE.md)

---

## 🎓 经验教训

1. **Cloudflare D1 的 FTS5 有限制**
   - 不能直接替换传统 LIKE 查询
   - 需要深入理解 D1 的实现细节

2. **渐进式优化很重要**
   - 智能 LIKE 搜索已经很好（0.426s）
   - 不必追求极致性能（0.05s）
   - 稳定性 > 性能

3. **充分测试再上线**
   - 100个案例覆盖不够
   - 应该测试 1000+ 案例
   - 边缘 case 很重要（如单字符搜索）

---

## ✅ 结论

**当前状态**: 生产环境已回滚到智能 LIKE 搜索模式

**性能**: ⭐⭐⭐⭐ 良好
- 平均响应: 0.426s
- 成功率: 99%
- P95: 0.657s

**用户体验**: 优秀
- 99% 查询 < 0.8秒
- 57.6% 查询 < 0.5秒
- 无极慢查询 (>1.5s)

**建议**: 保持当前方案，暂缓 FTS5 优化，转而研究 KV 倒排索引方案。
