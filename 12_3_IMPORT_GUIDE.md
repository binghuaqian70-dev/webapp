# 12.3数据汇总表批量导入指南

## 📋 概述

本指南说明如何从 AI Drive 批量导入 12.3数据汇总表的 2 个 CSV 分割文件到生产环境商品数据库。

---

## 📁 数据文件信息

### AI Drive 路径
```
/mnt/aidrive/12.3数据汇总表-utf8_part1.csv  (292行，包含表头)
/mnt/aidrive/12.3数据汇总表-utf8_part2.csv  (292行，包含表头)
```

### 文件统计
- **文件数量**: 2 个
- **总数据行数**: 582 行（291 + 291，排除表头）
- **文件大小**: 约 16KB 每个文件
- **表头格式**: `name,company_name,price,stock`

---

## 🚀 快速开始

### 1. 启动后台导入任务

```bash
cd /home/user/webapp
./start_12_3_import.sh
```

**输出示例**:
```
🚀 启动 12.3数据汇总表批量导入任务...
✅ 导入任务已在后台启动 (PID: 12345)
```

### 2. 查看实时进度

```bash
./check_12_3_import.sh
```

**输出示例**:
```
📊 12.3数据汇总表批量导入 - 实时进度监控
========================================

✅ 导入任务运行中 (PID: 12345)

📈 导入统计:
----------------------------------------
📁 文件进度: 1 / 2 (50.00%)
📦 分块进度: 2 / 4 (50.00%)
📊 当前文件: 12.3数据汇总表-utf8_part1.csv
📈 已导入记录: 150 条
📊 任务状态: running
🕐 开始时间: 2025-12-03T04:30:00.000Z
⏱️  已耗时: 2 分钟
⏱️  预计剩余: 0小时 2分钟 30秒
```

### 3. 持续监控进度（每 5 秒刷新）

```bash
watch -n 5 ./check_12_3_import.sh
```

### 4. 查看详细日志

```bash
# 实时查看导入日志
tail -f 12_3_import.log

# 查看后台输出
tail -f 12_3_import_nohup.log
```

---

## 📊 脚本功能特性

### ✅ 核心功能
1. **后台运行**: 使用 `nohup` 后台执行，不会因终端关闭而中断
2. **逐个导入**: 按文件顺序依次导入，确保稳定性
3. **智能分块**: 根据文件行数自动调整分块大小
4. **断点续传**: 支持从上次失败位置继续导入
5. **进度统计**: 实时记录文件级别和分块级别进度
6. **详细日志**: 记录每个操作的时间戳和结果

### 📦 智能分块策略

脚本会根据文件行数自动选择最优分块大小：

| 文件行数 | 分块大小 | 预计分块数 |
|---------|---------|-----------|
| > 100,000 | 100行 | ~1000块 |
| 50,000 - 100,000 | 120行 | ~500块 |
| 10,000 - 50,000 | 150行 | ~200块 |
| 1,000 - 10,000 | 200行 | ~50块 |
| < 1,000 | 300行 | ~3块 |

**12.3数据**: 每个文件 291行 → **每块 300行** → 每个文件 **1个分块**

### ⚙️ 配置参数

```javascript
const MAX_RETRIES = 3;              // 最大重试次数
const DELAY_BETWEEN_CHUNKS = 600;   // 分块间延迟 0.6秒
const DELAY_BETWEEN_FILES = 2000;   // 文件间延迟 2秒
const PROGRESS_SAVE_INTERVAL = 3;   // 每3个分块保存进度
```

---

## 📁 生成的文件

### 进度文件
- **12_3_import_progress.json**: 断点续传进度记录
- **12_3_import_stats.json**: 详细统计数据（JSON格式）

### 日志文件
- **12_3_import.log**: 详细导入日志（带时间戳）
- **12_3_import_nohup.log**: 后台进程输出日志

---

## 🔄 断点续传

如果导入过程中断（网络问题、服务器重启等），只需重新运行启动脚本：

```bash
./start_12_3_import.sh
```

脚本会自动检测进度文件并从上次中断的位置继续导入。

**断点续传日志示例**:
```
🔄 检测到断点续传: 已完成 1 个文件
📍 从第 2 个文件开始继续导入...
```

---

## 🛑 停止导入任务

### 方法 1: 使用 PID 停止

```bash
# 查看运行中的导入任务
pgrep -f "optimized_batch_import.mjs"

# 停止任务（替换 12345 为实际 PID）
kill 12345
```

### 方法 2: 强制停止

```bash
pkill -f "optimized_batch_import.mjs"
```

**注意**: 停止任务后，进度会保存在 `12_3_import_progress.json` 中，可以稍后继续导入。

---

## 📊 预计导入时间

### 时间估算

| 阶段 | 耗时 |
|------|------|
| 文件检查 | ~5秒（AI Drive 较慢） |
| 登录验证 | ~1秒 |
| part1 导入（291行） | ~2秒 |
| 文件间延迟 | 2秒 |
| part2 导入（291行） | ~2秒 |
| **总计** | **~12秒** |

**说明**: 12.3数据仅有 582 条记录（2个文件），预计 **12-15秒** 即可完成全部导入。

---

## ✅ 导入完成后

### 查看最终结果

```bash
./check_12_3_import.sh
```

**成功示例输出**:
```
📊 任务状态: completed
📈 已导入记录: 582 条
📋 文件导入结果:
----------------------------------------
  ✅ 12.3数据汇总表-utf8_part1.csv: 291 条记录
  ✅ 12.3数据汇总表-utf8_part2.csv: 291 条记录
```

### 验证数据库

访问生产环境验证数据：
```bash
curl -s "https://webapp-csv-import.pages.dev/api/products?page=1&limit=5" | jq '.pagination.total'
```

### 清理临时文件（可选）

导入成功后，可以删除临时文件：
```bash
rm -f 12_3_import_progress.json
rm -f 12_3_import_nohup.log
```

**保留日志和统计文件**:
- `12_3_import.log` (详细日志，建议保留)
- `12_3_import_stats.json` (统计数据，建议保留)

---

## ⚠️ 常见问题

### 1. 文件找不到

**错误**: `❌ 没有找到任何目标文件`

**解决方案**:
```bash
# 检查 AI Drive 文件
ls -lh /mnt/aidrive/ | grep "12.3数据汇总表"

# 确认文件路径
cat optimized_batch_import.mjs | grep "TARGET_FILES"
```

### 2. 登录失败

**错误**: `❌ 登录失败: 401 Unauthorized`

**解决方案**:
```bash
# 检查生产环境是否可访问
curl -I https://webapp-csv-import.pages.dev/api/health

# 检查用户名密码（在脚本中配置）
const USERNAME = 'admin';
const PASSWORD = 'admin123';
```

### 3. 导入速度慢

**原因**: AI Drive 是远程存储，读取较慢

**解决方案**:
- 保持耐心，AI Drive 文件读取需要 3-5 秒
- 脚本已优化，自动使用智能分块策略
- 查看日志确认进度: `tail -f 12_3_import.log`

### 4. 部分分块失败

**错误**: `❌ 分块 2 导入失败: HTTP 500`

**解决方案**:
- 脚本会自动重试（最多 3 次）
- 检查生产环境状态
- 查看详细错误日志: `grep ERROR 12_3_import.log`
- 重新运行脚本会从失败位置继续

---

## 🎯 导入流程图

```
开始
  ↓
检查 AI Drive 文件 (12.3数据汇总表-utf8_part1.csv, part2.csv)
  ↓
登录生产环境 (admin/admin123)
  ↓
获取数据库初始记录数
  ↓
━━━━━━━━━━━━━━━━━━━━━━━━━━━
  文件 1: part1.csv (291行)
━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ↓ 智能分块 (300行/块)
  ↓
分块 1 (1-291行) → 导入 API
  ↓ ✅ 成功
延迟 2 秒
  ↓
━━━━━━━━━━━━━━━━━━━━━━━━━━━
  文件 2: part2.csv (291行)
━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ↓ 智能分块 (300行/块)
  ↓
分块 1 (1-291行) → 导入 API
  ↓ ✅ 成功
  ↓
获取数据库最终记录数
  ↓
生成统计报告
  ↓
✅ 完成 (582条记录)
```

---

## 📞 支持

如遇到问题，请检查以下文件：

1. **详细日志**: `12_3_import.log`
2. **统计数据**: `12_3_import_stats.json`
3. **进度记录**: `12_3_import_progress.json`
4. **后台输出**: `12_3_import_nohup.log`

---

## 📝 相关文档

- [智能搜索性能报告](./SMART_SEARCH_PERFORMANCE_REPORT.md)
- [100案例性能测试](./100_CASES_PERFORMANCE_TEST_REPORT.md)
- [FTS5回滚报告](./FTS5_ROLLBACK_REPORT.md)
- [Worker防回收指南](./KEEP_WORKER_ALIVE_GUIDE.md)

---

**版本**: 1.0.0  
**更新时间**: 2025-12-03  
**适用数据**: 12.3数据汇总表 (2个分割文件，共582条记录)
