# 7.8数据汇总表批量导入快速参考

## 📋 概述

本系统支持从 AI Drive 批量导入 `7.8数据汇总表-utf8_part1.csv` 到 `7.8数据汇总表-utf8_part20.csv` 共20个CSV文件到生产环境商品数据库。

## 🎯 核心特性

- ✅ **逐个文件导入**: 按文件顺序依次处理，避免并发问题
- ✅ **智能分块**: 根据文件内容行数自动调整分块大小
- ✅ **断点续传**: 支持中断后从上次位置继续导入
- ✅ **后台运行**: 使用 nohup 在后台运行，不受终端关闭影响
- ✅ **实时进度**: 实时统计导入进度和预估完成时间
- ✅ **详细日志**: 完整记录导入过程和错误信息
- ✅ **6位小数**: 支持价格字段6位小数精度

## 📁 文件说明

### 核心文件
- `import_7_8_batch.mjs` - 主导入脚本
- `start_7_8_import.sh` - 启动脚本（推荐使用）
- `check_7_8_import.sh` - 进度查看脚本

### 运行时生成文件
- `7_8_import.log` - 详细导入日志
- `7_8_import_stats.json` - 统计数据（JSON格式）
- `7_8_import_progress.json` - 断点续传进度文件
- `7_8_import_nohup.log` - nohup后台运行日志

## 🚀 快速开始

### 1. 准备文件

确保20个CSV文件已放置在 AI Drive 目录：
```bash
ls -lh /mnt/aidrive/7.8数据汇总表-utf8_part*.csv
```

文件命名格式（注意：无前导零）：
- `7.8数据汇总表-utf8_part1.csv`
- `7.8数据汇总表-utf8_part2.csv`
- ...
- `7.8数据汇总表-utf8_part20.csv`

### 2. 启动导入（推荐方式）

```bash
cd /home/user/webapp
./start_7_8_import.sh
```

脚本会自动：
- 检查AI Drive中的目标文件
- 显示文件信息和预计记录数
- 询问是否开始导入
- 在后台启动导入进程
- 显示初始日志

### 3. 查看进度

```bash
cd /home/user/webapp
./check_7_8_import.sh
```

显示信息包括：
- 文件处理进度（已处理/总数）
- 分块处理进度
- 数据导入进度
- 预估剩余时间
- 当前处理文件
- 最新日志输出

### 4. 实时监控日志

```bash
tail -f /home/user/webapp/7_8_import.log
```

## 📊 进度统计

### 查看统计数据
```bash
cat 7_8_import_stats.json | python3 -m json.tool
```

统计数据包括：
```json
{
  "totalFiles": 20,           // 总文件数
  "processedFiles": 5,        // 已处理文件数
  "currentFile": "xxx.csv",   // 当前文件
  "totalRecords": 500000,     // 预计总记录数
  "processedChunks": 150,     // 已处理分块数
  "totalChunks": 300,         // 总分块数
  "importedRecords": 250000,  // 已导入记录数
  "startTime": "2024-xx-xx",  // 开始时间
  "status": "running",        // 运行状态
  "fileResults": [...]        // 各文件导入结果
}
```

### 查看进度文件
```bash
cat 7_8_import_progress.json | python3 -m json.tool
```

## 🔄 断点续传

### 自动恢复
如果导入过程中断，重新运行启动脚本即可从断点继续：
```bash
./start_7_8_import.sh
```

脚本会自动检测进度文件并询问是否继续。

### 清除进度重新开始
```bash
rm -f 7_8_import_progress.json 7_8_import_stats.json 7_8_import.log
./start_7_8_import.sh
```

## 🛠️ 常用命令

### 进程管理
```bash
# 查看导入进程
ps aux | grep import_7_8_batch

# 停止导入进程
pkill -f import_7_8_batch.mjs

# 或使用PID停止
kill <PID>
```

### 日志管理
```bash
# 实时查看日志
tail -f 7_8_import.log

# 查看最后100行
tail -100 7_8_import.log

# 搜索错误信息
grep "ERROR" 7_8_import.log

# 查看nohup日志
tail -f 7_8_import_nohup.log
```

### 数据验证
```bash
# 查看数据库总记录数
curl -H "Authorization: Bearer <token>" \
  https://webapp-csv-import.pages.dev/api/products?page=1&pageSize=1
```

## ⚙️ 配置说明

在 `import_7_8_batch.mjs` 中可调整的参数：

```javascript
const MAX_RETRIES = 3;              // 最大重试次数
const DELAY_BETWEEN_CHUNKS = 600;   // 分块间延迟(毫秒)
const DELAY_BETWEEN_FILES = 2000;   // 文件间延迟(毫秒)
const PROGRESS_SAVE_INTERVAL = 3;   // 进度保存间隔(块数)
```

智能分块策略：
- 超大文件（>10万行）: 100行/块
- 大文件（5-10万行）: 120行/块
- 中等文件（1-5万行）: 150行/块
- 小文件（1千-1万行）: 200行/块
- 超小文件（<1千行）: 300行/块

## ⚠️ 注意事项

### AI Drive性能
- AI Drive是远程目录，读写速度较慢
- 首次读取文件时可能需要等待
- 建议在网络状况良好时进行导入

### 文件命名
- 确保文件名格式正确：`7.8数据汇总表-utf8_part{N}.csv`
- 注意：文件编号**无前导零**（part1, part2, ..., part20）
- 不是 part01, part02 格式

### 数据库连接
- 脚本默认连接生产环境数据库
- 确保网络连接稳定
- 建议在非高峰时段进行批量导入

### 断点续传
- 进度文件自动保存，无需手动操作
- 中断后必须使用相同的脚本版本继续
- 不要手动修改进度文件

## 📈 性能参考

根据历史导入数据：
- 平均处理速度：约 800-1200 条/分钟
- 单个文件导入时间：取决于文件大小和网络状况
- 20个文件总耗时：预计 2-5 小时（视数据量而定）

## 🐛 故障排查

### 问题1：找不到文件
```
❌ 错误: 未找到任何目标文件
```
**解决方案**：
1. 检查文件是否在 `/mnt/aidrive` 目录
2. 验证文件命名格式是否正确
3. 使用 `ls -lh /mnt/aidrive/7.8*.csv` 查看实际文件

### 问题2：登录失败
```
❌ 登录失败: HTTP 401
```
**解决方案**：
1. 检查脚本中的用户名密码是否正确
2. 确认生产环境URL可访问
3. 验证网络连接是否正常

### 问题3：导入中断
```
⚠️ 检测到导入进程未运行
```
**解决方案**：
1. 查看日志文件了解中断原因
2. 重新运行 `./start_7_8_import.sh` 从断点继续
3. 如需重新开始，先删除进度文件

### 问题4：进程卡住
```
导入进程长时间无响应
```
**解决方案**：
1. 查看最新日志确认是否真的卡住
2. 检查网络连接和数据库状态
3. 必要时停止进程，从断点重新开始

## 📞 支持信息

- 日志文件：`7_8_import.log`
- 统计数据：`7_8_import_stats.json`
- 进度文件：`7_8_import_progress.json`

如遇问题，请提供以上文件内容以便排查。

## ✅ 导入完成后

1. 查看最终统计报告
2. 验证数据库记录数
3. 清理临时文件（可选）
4. 备份导入日志和统计数据

```bash
# 导入完成后的清理（可选）
rm -f 7_8_import_progress.json  # 清理进度文件
# 保留日志和统计数据以备查询
```

---

**最后更新**: 2024-01-08
**版本**: 1.0
